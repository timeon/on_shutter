<%= render :partial => 'instruction', :collection => @contacts, :as => :contact %>

<% if logged_in? %>
	<%= render 'form' %>  
<% else %>
    <br><div onclick = "show_flash_login('add_contact_notice', 'fill up contact', '<%= new_contact_path %>' )">Star（开始）</div><br>      
<% end %>

<%= render :partial => 'shared/in_place_notice', :locals => {:notice_id=> 'add_contact_notice'} %>  	


