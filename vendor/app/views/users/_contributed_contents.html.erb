  <% contents_name = type.to_s.pluralize %>
  
  <%= render :partial => 'users/contents_summary', 
             :locals => {:prefix        => "",
             	         :contents_name => "#{contents_name}", 
             	         :contents_path => index_link ? send("#{contents_name}_path", {:tab => @user.display_name + "'s ", :user => @user}) : nil,
             	         :contents      => @user.send(contents_name).paginate(page: 1, per_page: 10) } %>
